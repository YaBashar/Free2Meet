import React from "react";

// Allow time interval selection for events
// Allow user to delete selected time intervals

const TimePicker = () => {
  return <div>TimePicker</div>;
};

export default TimePicker;
